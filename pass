p = 0
x = input('請輸入密碼: ')
if x == '5555':
	print('已登入')
else:
	while True:
		p = p+1
		if p == 1 :
			print('錯誤，還剩 2 次機會')
			x = input('請輸入密碼: ')
		if p == 2 :
			print('錯誤，還剩 1 次機會')
			x = input('請輸入密碼: ')
		if p == 3 :
			print('帳號鎖定')
			break

